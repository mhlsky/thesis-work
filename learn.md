# learn.md：船舶姿态预测项目入门说明

> 这份文档面向刚接触本项目的人。目标不是讲深度学习全部理论，而是让你知道：这个项目在做什么、为什么这么设计、每个创新点解决什么问题、后续看代码时应该重点看哪里。

---

## 1. 这个项目到底在做什么？

一句话：

> 根据过去一段时间的船舶控制量、风场量和运动状态，预测未来几秒船舶的运动姿态。

数据是 1 Hz 采样，也就是每秒一个点。默认任务是：

```text
看过去 128 秒 -> 预测未来 10 秒
```

模型输入：

```text
n, deltal, deltar, Vw, alpha_x, alpha_y, u, v, p, r, phi
```

模型输出：

```text
u, v, p, r, phi
```

其中：

| 字段 | 含义 | 类型 |
|---|---|---|
| `n` | 螺旋桨轴转速 | 控制量 |
| `deltal` | 左舵桨方位角 | 控制量 |
| `deltar` | 右舵桨方位角 | 控制量 |
| `Vw` | 风速 | 环境量 |
| `alpha_x` | 风攻角余弦 | 环境量 |
| `alpha_y` | 风攻角正弦 | 环境量 |
| `u` | 纵荡速度 | 状态量 / 预测目标 |
| `v` | 横荡速度 | 状态量 / 预测目标 |
| `p` | 横摇角速度 | 状态量 / 预测目标 |
| `r` | 艏摇角速度 | 状态量 / 预测目标 |
| `phi` | 横摇角 | 状态量 / 预测目标 |

---

## 2. 为什么要分 exog 和 state？

项目中会把输入分成两类：

```python
exog_cols = ["n", "deltal", "deltar", "Vw", "alpha_x", "alpha_y"]
state_cols = ["u", "v", "p", "r", "phi"]
```

### 2.1 exog 是什么？

`exog` 是 exogenous variables，意思是外生变量。它们不是模型要预测的船舶状态本身，而是影响船舶状态变化的外部驱动。

在本项目中：

- 转速 `n` 会影响船舶推进；
- 舵角 `deltal/deltar` 会影响转向和横向运动；
- 风速和风攻角会影响外部扰动。

### 2.2 state 是什么？

`state` 是船舶当前自身的运动状态，例如当前速度、角速度、横摇角。

预测未来状态时，历史状态很重要，因为船舶有惯性，不会突然从一个状态跳到另一个状态。

---

## 3. 什么是滑动窗口？

模型不能直接吃整个 CSV 文件。我们要把长序列切成很多训练样本。

例如：

```text
seq_len = 128
pred_len = 10
```

一个样本就是：

```text
输入 x：第 0 秒到第 127 秒
标签 y：第 128 秒到第 137 秒
```

下一个样本可以是：

```text
输入 x：第 1 秒到第 128 秒
标签 y：第 129 秒到第 138 秒
```

这就叫滑动窗口。

关键限制：**窗口不能跨 CSV 文件**。每个 CSV 是独立仿真片段，不能把两个文件拼起来。

---

## 4. 为什么要有 baseline？

Baseline 是对照模型。没有 baseline，就无法证明你的模型好。

本项目建议的 baseline：

| 模型 | 作用 |
|---|---|
| Persistence | 最简单基线，未来都等于最后一个历史状态 |
| LSTM | 常规循环网络基线 |
| GRU | 更轻量的循环网络基线 |
| TCN | 卷积时序模型基线 |
| Transformer Encoder | 标准注意力模型基线 |

### 4.1 Persistence 为什么重要？

如果船舶运动变化比较慢，简单复制最后一个值也可能有不错结果。你的深度模型必须超过它，才说明模型真的学到了东西。

### 4.2 LSTM 和 GRU 的区别

LSTM 和 GRU 都是循环网络。

- LSTM 门控更多，表达能力强；
- GRU 结构更简单，参数更少，训练可能更快。

### 4.3 TCN 是什么？

TCN 是 Temporal Convolutional Network，时间卷积网络。它用一维卷积看历史序列。优点是并行性好，训练快；缺点是长期依赖能力依赖卷积层数和膨胀系数。

### 4.4 Transformer 为什么作为基线？

Transformer 用自注意力找历史窗口中重要时间点。它通常表达能力强，但标准注意力复杂度高，窗口长时计算量大。本项目最终用线性注意力，就是为了保留注意力能力，同时降低复杂度。

---

## 5. xLSTM 是什么？

LSTM 的核心思想是“记忆”。它通过门控决定：

- 新信息写入多少；
- 旧记忆保留多少；
- 输出多少隐藏状态。

xLSTM 可以理解为对 LSTM 的增强。它强调更强的门控、更稳定的记忆更新，以及更适合长序列的结构。

本项目不强制复现完整论文版 xLSTM，而是实现一个适合实验的 Lite-xLSTM：

```text
指数门控 + 归一化记忆更新
```

核心公式可以理解为：

```text
新记忆 = 旧记忆的一部分 + 新输入的一部分
```

但权重不是普通 sigmoid 门，而是指数门控：

```text
i_t = exp(...)
f_t = exp(...)
```

这样模型对“写入”和“遗忘”的控制更灵活。

---

## 6. 创新点一：CCG 控制/风场条件门控

### 6.1 这个创新解决什么问题？

船舶运动不是只由历史姿态决定的，还强烈受转速、舵角、风场影响。

普通 xLSTM 的门控大致是：

```text
门控 = f(当前输入, 历史隐藏状态)
```

但在船舶场景中，我们希望：

```text
门控 = f(历史状态, 历史隐藏状态, 当前控制量, 当前风场)
```

这样模型看到舵角突变、风速突变时，可以自动调整记忆更新。

### 6.2 怎么实现？

把每个时间步拆成：

```python
exog_t = [n, deltal, deltar, Vw, alpha_x, alpha_y]
state_t = [u, v, p, r, phi]
```

先用 MLP 编码外生量：

```text
context_t = MLP(exog_t)
```

再让 `context_t` 参与 xLSTM 门控：

```text
i_pre = W_i[state_t, h_prev] + U_i context_t
f_pre = W_f[state_t, h_prev] + U_f context_t
```

这就是 CCG：Control-Conditioned Gating。

### 6.3 论文里怎么说？

可以写：

> 针对船舶运动受推进器、舵角和风场强驱动的特点，本文设计控制/环境条件门控机制，将外生航行控制量引入 xLSTM 指数门控过程，使模型能够根据工况变化自适应调整记忆更新强度。

---

## 7. 创新点二：VMD 分解辅助监督

### 7.1 VMD 是什么？

VMD 是 Variational Mode Decomposition，变分模态分解。

直观理解：把一条复杂曲线拆成几个更简单的分量。

比如船舶横摇角 `phi` 可能包含：

```text
低频趋势 + 中频周期波动 + 高频扰动
```

VMD 就是尝试把它拆出来。

### 7.2 为什么适合船舶运动？

船舶运动受海浪、风、控制输入影响，序列往往是非平稳的。直接预测原始序列，模型要同时学习趋势、周期和噪声，难度大。

VMD 可以帮助模型分工学习：

```text
一个分支学趋势
一个分支学周期
一个分支学扰动
```

### 7.3 为什么不把 VMD 结果当输入？

因为如果你对整条测试序列做 VMD，再把分解结果作为输入，分解过程可能已经用到了未来信息。这会导致测试结果虚高。

所以本项目采用更安全的方式：

```text
VMD 只作为辅助标签，不作为模型输入
```

模型输出 K 个模态：

```text
mode_preds: [B, pred_len, 5, K]
```

最终预测：

```text
y_hat = mode_preds.sum(dim=-1)
```

损失：

```text
loss = MSE(y_hat, y_true) + lambda_vmd * MSE(mode_preds, y_modes)
```

---

## 8. 可选增强：线性注意力

### 8.1 注意力解决什么问题？

历史 128 秒里，不是每一秒都一样重要。

可能最关键的是：

- 舵角突然变化的时刻；
- 风速突然变化的时刻；
- 横摇角速度达到峰值的时刻；
- 船舶状态发生明显转折的时刻。

注意力机制就是让模型自动找这些关键时刻。

### 8.2 为什么用线性注意力？

标准 Transformer 注意力复杂度是：

```text
O(L²)
```

窗口越长，计算越重。

线性注意力通过核映射近似注意力，把复杂度降低，更适合仿真系统的低延迟需求。

本项目采用：

```text
phi(x) = elu(x) + 1
```

---

## 9. 创新点四：Delta Decoder 增量解码

### 9.1 直接预测有什么问题？

如果模型直接预测未来绝对状态：

```text
y_1, y_2, ..., y_H
```

可能出现不连续跳变。

但船舶有惯性，运动应该连续变化。

### 9.2 增量解码怎么做？

让模型预测变化量：

```text
Δy_1, Δy_2, ..., Δy_H
```

然后从历史最后状态开始累积：

```text
y_hat_1 = y_last + Δy_1
y_hat_2 = y_hat_1 + Δy_2
...
```

这样预测天然更连续。

---

## 10. 创新点五：State Coupling Mixer 状态耦合

### 10.1 为什么需要状态耦合？

预测目标有 5 个：

```text
u, v, p, r, phi
```

它们不是独立变量。

例如：

- `p` 是横摇角速度；
- `phi` 是横摇角；
- 理论上 `phi` 的变化和 `p` 有关系；
- `v` 和 `r` 也可能因为横荡/艏摇耦合相关。

### 10.2 怎么实现？

在每个预测步上，对 5 个输出做一个小的 MLP 修正：

```text
y_new = y + MLP(y)
```

这就是 State Coupling Mixer。

它很轻量，但能让模型学习输出变量之间的关系。

---

## 11. 创新点六：物理约束损失

深度学习模型可能预测出误差不大但物理上不合理的曲线，比如突然尖峰、角度跳变。

本项目用两个简单但有效的物理约束。

### 11.1 平滑约束

船舶有惯性，不应剧烈跳变。

约束二阶差分：

```text
y_{t+1} - 2y_t + y_{t-1}
```

这个值越大，说明曲线越不平滑。

### 11.2 横摇运动学一致性

数据中有：

```text
p   = 横摇角速度
phi = 横摇角
```

1 Hz 采样下，近似关系：

```text
phi_{t+1} - phi_t ≈ p_t * 1s
```

加入这个约束，可以让 `phi` 和 `p` 的预测更一致。

---

## 12. 什么是 OOD？

OOD 是 Out-of-Distribution，分布外测试。

本项目有两类测试：

```text
routine test：常规工况测试
OOD test：分布外工况测试
```

OOD 更难，因为它和训练集分布不完全一样。模型如果在 OOD 上表现好，说明泛化能力更强。

注意：OOD 只能最终测试，不能拿来调参或早停。

---

## 13. 什么是消融实验？

消融实验就是一个一个去掉模块，看性能变化。

本项目建议：

| 实验 | 目的 |
|---|---|
| LSTM | 常规基线 |
| GRU | 轻量 RNN 基线 |
| TCN | 卷积时序基线 |
| Transformer | 注意力基线 |
| Lite-xLSTM | 验证 xLSTM 指数门控 |
| CCG-xLSTM | 验证控制/风场条件门控 |
| VMD-CCG-xLSTM | 验证 VMD |
| VMD-CCG-Phys-xLSTM | 验证物理约束和最终主模型 |
| VMD-CCG-Attn-Phys-xLSTM | 可选：验证线性注意力增强 |

如果最终模型比前面逐步变好，就说明各个创新点是有贡献的。

---

## 14. 常用指标怎么看？

### 14.1 MAE

平均绝对误差。

```text
越小越好
```

### 14.2 RMSE

均方根误差。对大误差更敏感。

```text
越小越好
```

### 14.3 R²

决定系数，表示模型解释数据变化的能力。

```text
越接近 1 越好
```

### 14.4 物理指标

本项目建议额外看：

```text
smoothness
roll_consistency_rmse
```

它们不是普通误差指标，而是看预测是否更符合船舶运动规律。

---

## 15. 代码实现应该按什么顺序？

不要一上来写最终模型。推荐顺序：

1. 数据管线；
2. 常规 baseline：Persistence、LSTM、GRU、TCN、Transformer；
3. VMD 缓存和 `y_modes`；
4. Lite-xLSTM；
5. CCG-xLSTM；
6. VMD-CCG-xLSTM；
7. Delta Decoder 和 State Mixer；
8. 物理约束；
9. 线性注意力（可选增强）；
10. 消融实验和绘图。

每一步都要先跑 smoke test，再跑全量实验。

---

## 16. 最容易踩的坑

### 16.1 标准化泄漏

Scaler 只能在 train 上 fit，不能用 validation/test/OOD。

### 16.2 VMD 泄漏

VMD 结果不要作为测试输入特征。主线只作为辅助标签。

### 16.3 跨 CSV 滑窗

不能跨文件生成窗口。

### 16.4 物理损失尺度错误

物理损失要在反标准化后的真实物理尺度上算。

### 16.5 一次开太多模块

不要一开始同时打开 VMD、CCG、delta、mixer、physics。主线稳定后，如果还要做 attention，再单独打开，方便定位问题。

---

## 17. 最终你应该怎么理解这个模型？

可以把最终模型理解成：

```text
1. CCG-xLSTM 负责根据船舶状态、控制量、风场量提取历史动态特征；
2. VMD 多分支头负责把未来运动拆成不同模态去预测；
3. Delta Decoder 保证未来预测是连续累积出来的；
4. State Mixer 负责修正多个状态量之间的耦合关系；
5. 物理损失负责约束预测曲线不要违背基本运动规律；
6. 线性注意力是最后可选增强，用于从历史窗口里挑关键时刻。
```

这条路线的优点是：每个模块都能解释清楚，并且都和船舶运动预测场景相关。


